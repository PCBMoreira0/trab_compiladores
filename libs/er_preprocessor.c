char *preProcess(const char *input) {
    // Implementation for pre-processing the input regex

    for(int i = 0; input[i] != '\0'; i++) {
        char c = input[i];
        if(c == '['){
            
        }
    }
}